<UserControl x:Class="CMiX.Studio.Views.Project"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ViewsScheduler="clr-namespace:CMiX.Studio.Views.Scheduler"
             mc:Ignorable="d" d:DesignWidth="542.666" d:DesignHeight="388">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0.7*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <ViewsScheduler:PlaylistEditor Margin="0, 0, 2, 0" DataContext="{Binding Path=CompositionScheduler.PlaylistEditor}"/>
            <ViewsScheduler:JobEditor Grid.Column="1" Margin="2, 0, 0, 0" DataContext="{Binding Path=CompositionScheduler.JobEditor}"/>
        </Grid>
        <ViewsScheduler:Scheduler Grid.Row="1" Margin="0, 4, 0, 0" DataContext="{Binding Path=CompositionScheduler.JobScheduler}"/>
    </Grid>

</UserControl>