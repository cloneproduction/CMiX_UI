<UserControl x:Class="CMiX.Studio.Views.Scheduler.CompositionScheduler"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CMiX.Studio.Views.Scheduler"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="0.7*"/>
        </Grid.RowDefinitions>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <local:PlaylistEditor Margin="2" DataContext="{Binding PlaylistEditor}"/>
            <local:JobEditor Grid.Column="1" Margin="2" DataContext="{Binding JobEditor}"/>


        </Grid>
        <local:Overview Grid.Row="1" Margin="2" DataContext="{Binding JobScheduler}"/>
    </Grid>

</UserControl>
