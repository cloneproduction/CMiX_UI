<UserControl x:Class="CMiX.Studio.Views.ImageSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewModels="clr-namespace:CMiX.Studio.ViewModels"
             xmlns:Views="clr-namespace:CMiX.Studio.Views"
             xmlns:Controls="clr-namespace:CMiX.MVVM.Controls;assembly=CMiX.MVVM"
             xmlns:dd="urn:gong-wpf-dragdrop"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <ComboBox 
        Margin="0, 2"
        Height="25" 
        ItemsSource="{Binding AssetsView}"
        SelectedItem="{Binding SelectedAsset}"
        AllowDrop="True"
        dd:DragDrop.DropHandler="{Binding}"
        dd:DragDrop.IsDropTarget="True">

        <!--<ComboBox.ItemContainerStyle>
            <Style TargetType="ComboBoxItem" BasedOn="{StaticResource Default}">
                <Setter Property="IsSelected" Value="{Binding IsInUse, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </Style>
        </ComboBox.ItemContainerStyle>-->

        <ComboBox.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Name}"/>
            </DataTemplate>
        </ComboBox.ItemTemplate>

        <ComboBox.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel RequestBringIntoView="OnRequestBringIntoView"/>
            </ItemsPanelTemplate>
        </ComboBox.ItemsPanel>

    </ComboBox>
</UserControl>