<UserControl x:Class="CMiX.Studio.Views.RangeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Views="clr-namespace:CMiX.Studio.Views"
             xmlns:ViewModels="clr-namespace:CMiX.MVVM.ViewModels;assembly=CMiX.MVVM"
             xmlns:MVVM.Views="clr-namespace:CMiX.MVVM.Views;assembly=CMiX.MVVM"
             xmlns:Controls="clr-namespace:CMiX.MVVM.Controls;assembly=CMiX.MVVM"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="108.416" d:DesignWidth="380.64">

    <UserControl.Resources>
        <ObjectDataProvider x:Key="RangeModifier" MethodName="GetNames" ObjectType="{x:Type System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="ViewModels:RangeModifier"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <StackPanel>
        <Grid Height="20" Margin="0,0, 0, 2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition  Width="0.9*"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Label Content="Type" HorizontalAlignment="Right" Padding="8, 0" Foreground="{StaticResource TextBase}"/>
            <ComboBox
                Grid.Column="1"
                Margin="1, 0, 0, 0"
                SelectedItem="{Binding Modifier}"
                SelectedIndex="0"
                ItemsSource="{Binding Source={StaticResource RangeModifier}}"/>
        </Grid>

        <Grid Margin="0, 0, 0, 1" Height="25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.9*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Controls:DistanceSlider Position="Top"  Grid.Column="1" Style="{StaticResource DistanceSliderStyle}" Value="{Binding MinimumRange}" />
            <TextBlock Grid.Column="0" Padding="8, 0" HorizontalAlignment="Right" Text="Min" Foreground="{StaticResource TextBase}"/>
        </Grid>
        <Grid Height="25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.9*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Controls:DistanceSlider Position="Bottom" Grid.Column="1" Style="{StaticResource DistanceSliderStyle}" Value="{Binding MaximumRange}" />
            <TextBlock Grid.Column="0" Padding="8, 0" HorizontalAlignment="Right" Text="Max" Foreground="{StaticResource TextBase}"/>
        </Grid>

    </StackPanel>
</UserControl>