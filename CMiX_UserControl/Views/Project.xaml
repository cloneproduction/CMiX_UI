<UserControl x:Class="CMiX.Views.Project"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Views="clr-namespace:CMiX.Views"
             xmlns:ViewModels="clr-namespace:CMiX.Studio.ViewModels"
             xmlns:Resources="clr-namespace:CMiX.MVVM.Resources;assembly=CMiX.MVVM"
             xmlns:MVVM.Views ="clr-namespace:CMiX.MVVM.Views;assembly=CMiX.MVVM"
             xmlns:dd="urn:gong-wpf-dragdrop"
             mc:Ignorable="d"
             d:DesignHeight="770" d:DesignWidth="1345.334">

    <UserControl.InputBindings>
        <KeyBinding 
            Modifiers="Control"
            Key="T"
            Command="{Binding AddCompoCommand}" />
        <KeyBinding 
            Modifiers="Control"
            Key="N" 
            Command="{Binding AddLayerCommand}" />
        <KeyBinding 
            Modifiers="Control"
            Key="E" 
            Command="{Binding ExportCompoCommand}" />
        <KeyBinding 
            Modifiers="Control"
            Key="I" 
            Command="{Binding ImportCompoCommand}" />
    </UserControl.InputBindings>

    <UserControl.Resources>
        <ResourceDictionary>
            <Resources:BindingProxy x:Key="proxy" Data="{Binding}"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <DockPanel Background="{StaticResource SuperHighDarkerColor}">
        <Border DockPanel.Dock="Bottom" VerticalAlignment="Center" Background="{StaticResource DarkColor}" Height="25"/>
        <Border 
            Padding="2"
            DockPanel.Dock="Top"
            Background="{StaticResource DarkColor}" >
            <Menu
                Margin="16, 0, 0, 0"
                HorizontalAlignment="Left"
                VerticalContentAlignment="Center">
                <MenuItem
                    Padding="8,0"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="center"
                    Header="File">
                    <MenuItem
                        Header="New"
                        Command="{Binding NewProjectCommand}"
                        Template="{StaticResource SubMenuItemBase}" />
                    <MenuItem
                        Header="Open"
                        Command="{Binding OpenProjectCommand}"
                        Template="{StaticResource SubMenuItemBase}" />
                    <MenuItem
                        Header="Save"
                        Command="{Binding SaveProjectCommand}"
                        Template="{StaticResource SubMenuItemBase}" />
                    <MenuItem
                        Header="Save As"
                        Command="{Binding SaveAsProjectCommand}"
                        Template="{StaticResource SubMenuItemBase}" />
                    <MenuItem
                        Header="Quit"
                        Command="{Binding QuitCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}"
                        Template="{StaticResource SubMenuItemBase}" />
                </MenuItem>
                <MenuItem
                    Padding="8,0"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="center"
                    Header="Edit">
                    <MenuItem
                        Header="Undo    Ctrl + Z"
                        Template="{StaticResource SubMenuItemBase}"
                        Command="{Binding UndoCommand}"/>
                    <MenuItem
                        Header="Redo    Ctrl + Y"
                        Template="{StaticResource SubMenuItemBase}" 
                        Command="{Binding RedoCommand}"/>
                </MenuItem>
                <MenuItem
                    Padding="8,0"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="center"
                    Header="Compo">
                    <MenuItem
                        Header="Add"
                        Command="{Binding CompositionEditor.NewCompositionCommand}"
                        Template="{StaticResource SubMenuItemBase}" />
                    <MenuItem
                        Header="Delete"
                        Command="{Binding DeleteCompoCommand}"
                        CommandParameter="{Binding SelectedComposition}"
                        Template="{StaticResource SubMenuItemBase}" />
                    <MenuItem
                        Header="Duplicate"
                        Command="{Binding DuplicateSelectedCompositionCommand}"
                        CommandParameter="{Binding SelectedComposition}"
                        Template="{StaticResource SubMenuItemBase}" />
                    <Separator BorderThickness="0.5" Margin="0, 8, 8, 8" BorderBrush="{StaticResource DarkColor}"/>
                    <MenuItem
                        Header="Import"
                        Command="{Binding ImportCompoCommand}"
                        Template="{StaticResource SubMenuItemBase}" />
                    <MenuItem
                        Header="Export"
                        Command="{Binding ExportCompoCommand}"
                        Template="{StaticResource SubMenuItemBase}" />
                </MenuItem>
            </Menu>
        </Border>
        <Grid Background="{StaticResource SuperHighDarkerColor}" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.2*"/>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border 
                Grid.Column="2"
                Margin="0">
                <Views:CompositionEditor DataContext="{Binding CompositionEditor}"/>
            </Border>
            <Border>
                <TabControl TabStripPlacement="Left" Background="{StaticResource DarkerColor}" Style="{StaticResource TabControlVerticalPanel}" >
                    <TabItem Margin="8, 8, 8, 4" Background="Red" Width="25" Height="25" Style="{StaticResource TabItemVerticalPanel}">
                        <TabItem.Header>
                            <Image Source="{StaticResource Composition}" />
                        </TabItem.Header>
                        <Views:CompositionPanel  DataContext="{Binding CompositionEditor}"/>
                    </TabItem>
                    <TabItem Margin="8, 8, 8, 4" Width="25" Height="25" Style="{StaticResource TabItemVerticalPanel}">
                        <TabItem.Header>
                            <Image Source="{StaticResource Resources}" />
                        </TabItem.Header>
                        <Views:TreeView  DataContext="{Binding Assets}" Grid.RowSpan="2"/>
                    </TabItem>
                    <TabItem Margin="8, 4"  Width="25" Height="25" Style="{StaticResource TabItemVerticalPanel}">
                        <TabItem.Header>
                            <Image Source="{StaticResource Network}"/>
                        </TabItem.Header>
                        <Views:ServerManager DataContext="{Binding ServerManager}"/>
                    </TabItem>
                </TabControl>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>