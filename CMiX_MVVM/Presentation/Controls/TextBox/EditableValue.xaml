<UserControl x:Class="CMiX.Core.Presentation.Controls.EditableValue"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CMiX.Core.Presentation.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <TextBlock 
            x:Name="TextDisplay" 
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Text="{Binding Path=Text,RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:EditableValue}}}" />
        <TextBox
            x:Name="InputValue" 
            SelectionBrush="{StaticResource SuperHighLightColor}"
            CaretBrush="{StaticResource SuperHighLightColor}"
            Padding="5"
            BorderThickness="0"
            VerticalAlignment="Center"
            PreviewTextInput="TextInput_PreviewTextInput"
            Background="{StaticResource HighDarkerColor}" 
            Foreground="{StaticResource ActiveTextBase}" 
            Text="{Binding Path=Text, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:EditableValue}}}" />
    </Grid>

</UserControl>
